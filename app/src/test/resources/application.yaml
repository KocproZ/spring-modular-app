spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
  flyway:
    enabled: true
  jpa:
    show-sql: false
